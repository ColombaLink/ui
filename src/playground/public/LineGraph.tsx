import React from 'react'
import { LineGraph as LG } from '~'
import ComponentViewer from '../ComponentViewer'

export const LineGraph = () => {
  return <ComponentViewer component={LG} propsName="LineGraphProps" />
}
